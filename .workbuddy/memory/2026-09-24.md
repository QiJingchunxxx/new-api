# 2026-09-24 工作日志

## 贡献上游 Key（Contributed Upstream Key）功能 + 首页落地页改版

### 后端（Go）
- `setting/operation_setting/contributed_key_setting.go`（新增）：配置组 `contributed_key_setting`，
  字段 enabled / reward_amount / daily_cap_amount / reward_per_key / reset_hour / verify_interval_min /
  auto_create_channel / channel_group / channel_weight / channel_tag / max_keys_per_user / providers(JSON)。
  内置供应商预设：sensenova（商汤 SenseNova，base_url `https://token.sensenova.cn/v1`，OpenAI 兼容，渠道类型 1）。
- `model/contributed_key.go`（新增）：`contributed_keys` + `contributed_key_grants` 两张表（已加入 `model/main.go` AutoMigrate）。
  关键函数：RecalcUserContributionQuota（当天额度重算，差额用 DeltaUpdateUserQuota 补齐/扣回）、
  RolloverContributionGrants（跨天回收，先置 settled 再扣额度保证幂等）、ContributedBusinessDate（支持 reset_hour 业务日）。
  唯一索引为 (provider, key_hash)，Key 明文保存（需写入渠道池），列表接口返回掩码。
- `service/contributed_key.go`（新增）：验证走 `GET {base_url}/models`，可选用 `verify_model` 做 1 token 对话探测；
  验证通过后 `EnsureContributedKeyChannel` 自动建/更新渠道（tag=contributed，AutoBan=0），失效时 `DisableContributedKeyChannel`。
- `service/contributed_key_task.go`（新增）：1 分钟 tick 的主节点任务，负责跨天回收 + 周期验证 + 补发额度；
  在 `main.go` 中通过 `service.StartContributedKeyTask()` 启动。
- `controller/contributed_key.go`（新增）+ 路由：
  - 用户端 `/api/user/contributed_keys`（GET / POST / :id/verify / DELETE :id）
  - 管理端 `/api/contributed_keys`（GET `/`、`/stats`、POST `/verify`、`/recalculate`、PUT/DELETE `/:id`）
- `setting/operation_setting/home_landing_setting.go`（新增）：落地页装修配置组 `home_landing_setting`，
  文案默认空串 → 前端回退到内置多语言默认值；公开接口 `GET /api/home_landing`（controller/misc.go: GetHomeLandingConfig）。

### 前端（React）
- 侧边栏「常规」分组在 API 密钥后新增「贡献上游 Key」→ `/contributed-keys`（`hooks/use-sidebar-data.ts`）。
- `features/contributed-keys/`：我的贡献 / 全部 Key（管理员）/ 设置（超级管理员）三个 Tab。
  设置页通过 `PUT /api/option/` 写 `contributed_key_setting.*`（需 RootAuth）。
- `routes/_authenticated/contributed-keys/index.tsx` + 手工补齐 `routeTree.gen.ts`（13 处，rsbuild 构建时会自动重生成）。
- 首页改版：Hero 改为居中主视觉（渐变标题 + 终端演示）、Stats 改为实时数据条、新增 ModelGallery（数据来自 /api/pricing）、
  Faq、Community 区块；顺序 Hero → Stats → ModelGallery → HowItWorks → Features → Faq → Community → CTA → Footer。
- 后台装修入口：系统设置 → 内容 → 「Landing Page」（`features/system-settings/content/home-landing-section.tsx`），
  表驱动 FIELD_GROUPS，键前缀 `home_landing_setting.`。
- i18n：向 `web/src/i18n/locales/zh.json` 追加约 130 条中文翻译（在 translation 对象末尾追加，未重排既有键）。

### 验证情况
- 前端类型检查（外挂式）：临时 `cd web && npm install --legacy-peer-deps`（首次报 edgesOut，重试成功），
  再跑 `./node_modules/.bin/tsgo -b`，过滤测试文件后**本次改动的文件零报错**；
  `src/features/channels/api.ts` 的 axios 参数类型报错是 npm 依赖版本漂移造成的既有噪声。
  rsbuild build 在 npm 依赖树下跑不通（缺 antd / @lobehub/ui 等 peer），验证后已清理 node_modules、package-lock.json、dist。
- 后端无 Go 工具链，未编译；已用脚本核对新文件的括号配平与 import 使用情况，并逐项核对了 GORM / logger / common 的调用签名。
- `src/routeTree.gen.ts` 手工补的 13 处已由 rsbuild 的 tanstackRouter 插件重新生成覆盖，diff 为 +23 行，与生成结果一致。

### 注意事项
- 本机没有 Go 工具链与 bun；`npm install` 首次会报 `edgesOut` 错误（重试可成功），但 npm 依赖树与 bun 不一致，
  **不能用来出包**。真正部署需在有 bun 的机器执行 `cd web && bun install && bun run build`（Go 通过 go:embed 打包 web/dist）。
- 新增 i18n 文案时统一走脚本批量追加，避免手改 7000 行的 zh.json。
- 并发安全：`RecalcUserContributionQuota` 用包级互斥锁 + 发放记录行上的比较并交换（`WHERE id=? AND quota=?`）防重复发放；
  `RolloverContributionGrants` 先置 settled 再扣额度，保证任务重试幂等。
- 已沉淀技能：`.workbuddy/skills/newapi-feature-extension/SKILL.md`（新增功能的标准流程与踩坑清单）。

## 第二轮：改成「共享号池」+ 隐藏 Key 与内部信息 + FAQ 页 + 钱包每日额度

### 关键设计决策：用 new-api 原生多 Key 轮询做共享号池
- 放弃「一个贡献 Key 一个渠道」，改为**每个供应商一个号池渠道**：`ChannelInfo.IsMultiKey=true`、
  `MultiKeyMode=constant.MultiKeyModePolling`、Key 用 `\n` 拼接、`AutoBan=1`。
- 轮询：`Channel.GetNextEnabledKey()` 在 polling 模式下按 `MultiKeyPollingIndex` 顺序取 Key。
- **无痕切换的前提**：`service.DisableChannel` 只在 `channelError.AutoBan` 为 true 时才生效，
  且多 Key 渠道下 `handlerMultiKeyUpdate` 只禁用出错的**那一个 Key 索引**（`MultiKeyStatusList`），
  请求随即重试到下一个 Key；因此号池渠道必须 `AutoBan=1`（第一轮写成 0 是错的，已修）。
- **另一个硬前提**：`common.RetryTimes` 默认是 0 → 失败根本不重试，用户会直接看到错误。
  已做成管理端可见告警 + 一键把 `RetryTimes` 设为 2（`PUT /api/option/`），并在号池卡片里提示。
- 号池写入走 `model.SaveContributedPoolChannel`：**刻意不用** `Channel.Update` / `UpdateChannelStatus`，
  因为那两个入口会给管理员推「渠道已禁用/已启用」通知，Key 频繁进出会刷屏；改为 `Select(...).Updates(Channel{...})`
  （保证零值也写入、`channel_info` 走 schema 序列化）+ `UpdateAbilities` + `CacheUpdateChannel`。
- 变更后调用 `model.InitChannelCache()`，否则新号池要等下一次 `SyncChannelCache` 才可调度。
- 复检任务结束后 `SyncContributedPools(..., heal=true)`：清掉失效 Key，并把请求链路临时禁用的 Key 静默放回。
  Key 列表变化时索引会错位，必须整体丢弃 `MultiKeyStatusList` 重来。

### 对用户隐藏 Key 与元提示
- 用户端与管理端 payload 分离：`contributedKeyUserPayload`（id/provider/掩码/状态/最近验证）
  vs `contributedKeyAdminPayload`（额外含用户、原始上游报错、模型列表）。
- 新增 `reason_code` 列（auth/quota/network/upstream/format/config/unknown），用户端只看到分类文案，
  上游原始报错仅管理端可见；供应商 payload 只下发 name + docs_url，不再下发 base_url。
- 号池渠道信息（channel_id、disabled_keys、模型数）只在管理端统计里出现。
- 渠道标签改为 `<channel_tag>:<provider>`（默认 `contributed-pool:sensenova`）用于反查号池。

### 前端新增
- 钱包页 `features/wallet/components/daily-quota-card.tsx`：按供应商展示「xxx 每日额度」卡片 + 今日已发放 + 上限状态。
- `features/faq/`：公开使用指南页（`/faq`），左侧问答 + 右侧粘性「快速上手 / 还有疑问」，
  问答内容与首页 FAQ 共用后台配置（`home_landing_setting.faq_items`），内置默认搬到 `features/faq/constants.ts`。
- 顶部导航新增 `help` 模块（`lib/nav-modules.ts` 默认 true）→ `/faq`。
- `routeTree.gen.ts` 手工补 `/faq/` 与 `/contributed-keys/`（共 21 处），已被 rsbuild 插件重生成覆盖验证。

### 第二轮验证
- 再次用 npm 临时装依赖跑 `tsgo -b`（过滤测试文件后）**本次改动的文件零类型错误**；
  只剩 `features/channels/api.ts` 与 `test-setup.ts` 两处由 npm 依赖版本漂移引起的既有噪声。验证后已清理 node_modules/package-lock.json/dist。
- Go 侧仍无工具链，未编译；已核对括号配平、import 使用、`UpdateContributedKeyVerification` 新签名与调用一致。

## Docker 部署答疑（用户反馈"服务器上改的东西都不见了"）
- 结论：不是被覆盖，而是**官方镜像里本来就没有源码**。`Dockerfile` 三段构建（bun 出 web/dist → golang 把 dist 一起编译成单个 `/new-api` → debian 只 COPY 这个二进制），
  而 `docker-compose.yml` 用的是 `image: calciumion/new-api:latest`（远程镜像），本地源码改动完全不参与。
- 会保留：`./data`、`./logs`、`pg_data/mysql_data` 卷，以及**后台 UI 里改的配置项（存在数据库 options 表）**。
- 会丢：容器内的任何文件改动、宿主机改了源码但没重建镜像的部分。
- 自定义改动上线方式：compose 加 `build: {context: ., dockerfile: Dockerfile}` 并 `image: my-new-api:custom`，然后 `docker compose build && up -d`；
  或本地/CI 构建后推自己的 registry。`docker-compose.dev.yml` 是本地开发用的（build + image: new-api-dev:local，前端走 5173 dev server）。
- 注意：`web/dist` 是 gitignore 的，`go:embed` 必须先生成它；Dockerfile 会自动构建前端，但直接在宿主机 `go build` 前必须先 `cd web && bun run build`。

### 已落地改动（二次开发分支的 Docker 部署）
- `docker-compose.yml`：new-api 服务改为 `build: {context: ., dockerfile: Dockerfile}` +
  `image: new-api-custom:latest`（本地 tag，Docker Hub 不存在，pull 不可能覆盖）+ `pull_policy: never`；
  其余服务（redis/postgres）与 environment、卷挂载原样保留；顺带修掉头部注释里失效的行号引用。
- 新增根目录 `deploy.sh`（LF、已 chmod +x）：`docker compose build [--pull] && up -d`，
  并打印镜像 ID/创建时间与容器状态，避免"忘记重建镜像"。
- 提醒用户：服务器上的 compose 里可能已改成真实密码，**只把这 4 行（build/image/pull_policy）搬过去**，不要整文件覆盖。
- `.github/workflows/docker-build.yml` 仍推 `calciumion/new-api`（上游 CI，fork 上会失败），未改动，仅提示可按需禁用。

### 从官方镜像切到自建镜像的迁移步骤（已告知用户）
- 不动数据库的最小操作：`docker compose rm -sf new-api` → `docker image rm calciumion/new-api:latest` → `docker compose up -d --build new-api`。
- **绝对不能加 `-v`**（`docker compose down -v` 会删掉 pg_data 等卷 = 删库）。
- 验证新代码是否生效的特有接口：`curl -s localhost:3000/api/home_landing`（官方镜像返回 404，本分支返回 JSON）。
- `deploy.sh` 已支持 `--clean`（删容器 + 删所有 `calciumion/new-api:*` 镜像 + `image prune -f`，并在检测到 watchtower 时告警）
  与 `--pull`，可组合；末尾做镜像自检（比对 `docker inspect new-api --format '{{.Config.Image}}'`）。
  已用打桩的假 docker 跑通全部分支（含负向：无容器/无镜像/跑的是官方镜像/非法参数 exit 1）。
- **执行顺序已改为「先 build → 再 clean → 最后 up」**：原顺序会在构建的十几分钟里删掉运行中的容器，造成不必要的停机。
- 用户反馈"部署很慢"的答案：慢的是从源码构建（首次拉 bun/golang/debian 基础镜像 + bun install + rsbuild + go mod download + go build，5-20 分钟），
  与 `--clean` 无关（image prune -f 不删 build cache，只删悬空镜像）。想恢复秒级部署要在本地/CI 构建后推自己的 registry，服务器只 pull。
  想加 BuildKit cache mount 加速需要改 Dockerfile，**未擅自改**，需先确认服务器 `docker buildx version`。

## 部署方案定稿：GitHub Actions 构建 → GHCR → 服务器只拉
- 约束确认：**服务器 1核2G 跑不动构建**；**开发机（Windows）没装 Docker/Podman** → 只能走 CI 构建。
- 仓库事实：fork `QiJingchunxxx/new-api`，分支 `main`；`ci.yml` 只在 PR 触发、`docker-build.yml` 只在打 tag 触发 → push 到 main 不会触发任何现有工作流，可安全新增。
- 新增 `.github/workflows/build-image.yml`：push main / 手动触发，构建后推 `ghcr.io/<owner 小写>/new-api:latest` + `sha-xxx`，
  用 GHA 层缓存（`cache-from/to: type=gha`），`platforms: linux/amd64`；Action 全部沿用上游固定的 commit SHA。
  **踩坑**：GHCR 名必须全小写，用 `${GITHUB_REPOSITORY_OWNER,,}` 转换，不能直接拼 `${{ github.repository_owner }}`。
- `docker-compose.yml` 改为双模式：`image: ${NEW_API_IMAGE:-new-api-custom:latest}` + `pull_policy: ${NEW_API_PULL_POLICY:-never}`，保留 `build:`；
  `.env.example` 追加了这两个变量的说明（`.env` 已被 gitignore）。
- `deploy.sh` 增加 `--registry`（只拉不构建），自检逻辑改为从 `.env` 读期望镜像名；顺序仍是「先构建/拉取 → 再清理 → 最后 up」。
- 已沉淀技能：`.workbuddy/skills/newapi-custom-deploy/SKILL.md`（含 GHCR 私有包、down -v、prune -a 等坑）。
- 用户待办：commit+push（当时有 43 个未提交文件）→ Actions 出镜像 → 服务器配 .env + `./deploy.sh --registry`；
  GHCR 包默认私有，需要改公开或在服务器 docker login。
- **首次启动后必做的两项配置**（否则功能看起来"没生效"）：
  ① 后台「贡献上游 Key → 设置」打开开关，并点「启用重试（2 次）」把 `RetryTimes` 从默认 0 改掉，否则号池无法无痕切换；
  ② 「系统设置 → 内容 → Landing Page」按需装修首页文案/FAQ/社区（留空即用内置默认）。
- 风险提示：若此前跑的官方镜像版本比源码新，切换后可能出现 schema 不匹配报错，需看日志确认。
- **服务器报「未知参数：--registry」的排查结论**：提交 `89dfe7373`（16:39:53）已含新版 deploy.sh/build-image.yml/compose，且已在 origin/main 上；
  服务器 16:49 跑到的仍是旧脚本 → 服务器那次 `git pull` 没成功（最可能是 docker-compose.yml 的本地密码改动挡住 pull，或 origin/分支不对）。
- **待办建议（用户未确认）**：把 postgres/redis 密码也改成 `.env` 变量（`POSTGRES_PASSWORD`/`SQL_DSN`/`REDIS_CONN_STRING`），
  让仓库里的 compose 保持干净，以后 `git pull` 不会再因本地改动失败。

### 服务器拉不到新代码的根因（已确认）
- 服务器 `git status -sb` = `## main...origin/main [behind 1]`，HEAD 停在 `d04c118c8`（= upstream/main），且工作区有 20+ 个 `M` 与大量 `??`
  （含 `?? deploy.sh`、`.workbuddy/`、我的全部新功能文件）→ **服务器那份代码是从开发机手工拷贝过去的**，
  拷贝时点约在我写 `--registry` 之前，所以 `grep -c registry deploy.sh` = 0。
- 修复：`git fetch origin && git stash push -u && git pull --ff-only`（这些未提交改动在 fork 的 89dfe7373 里已有，属冗余；stash 可回滚）。
- 已核实仓库不跟踪 `data/`、`logs/`，`.gitignore` 含 `logs`/`.env`/`/data/`/`web/node_modules`/`web/dist` → git 操作不会碰数据库与 .env。
- 铁律：服务器更新只走 `git pull`，禁止 WinSCP/宝塔 覆盖式上传代码（否则每次都会重演这个问题）。已写进 deploy 技能。

### 事故：`--registry` 因 .env 缺失回退成"服务器上编译"
- 服务器 git pull 成功（新 deploy.sh 生效 ✅），但 `/opt/new-api/.env` 没有 `NEW_API_IMAGE`，
  → compose 解析成 `image: new-api-custom:latest` + `pull_policy: never` → 本地无此镜像 + 有 `build:` 段 → **compose 直接开始在 1核2G 上编译**（bun install 已在跑）。
- 处置：Ctrl+C 中断（构建中断不影响正在运行的旧容器）；补 .env 后重跑。
- **已给 deploy.sh 加硬拦截**：`--registry` 模式下若 .env 没配 NEW_API_IMAGE（或值里不含 `/`，明显不是仓库地址）→ 直接报错退出，绝不回退到构建。已用打桩验证三个分支。
- 顺带移除 docker-compose.yml 里已废弃的 `version: '3.4'`（compose v2 会告警）。

### 最终定位：构建成功，卡在 GHCR 包私有
- 用匿名 API 查到：仓库是公开的 → `api.github.com/repos/QiJingchunxxx/new-api/actions/runs` 显示
  `Build custom image (GHCR)` = completed/**success**（08:40 UTC = 北京 16:40，sha 89dfe73）→ **镜像已构建并推送**。
- GHCR 匿名 token 请求返回空 token、tags/manifest 全部 403 → **包是私有**，服务器没权限拉。
- 用户此前 `docker login` 失败的原因：`echo "<ghp_xxx>"` 把占位符尖括号一起带进去了（令牌里不能有 `<>`）。
  ⚠️ 用户把真实 PAT 贴进了聊天，已提醒立即撤销；并告知以后用 `read -rsp` 输入避免进 history。
- 结论：只剩「改包为 Public」或「用 classic PAT(read:packages) 登录」二选一。诊断手法已写进 deploy 技能。

### ✅ 部署成功（2026-09-24 17:46）
- 用户把 GHCR 包改为 Public 后：`docker pull ghcr.io/qijingchunxxx/new-api:latest` → Pulled；
  `./deploy.sh --registry` → 容器 Started，自检输出 `OK：运行中的镜像为 ghcr.io/qijingchunxxx/new-api:latest`。
- 至此「1核2G 服务器不编译、GitHub Actions 出镜像、服务器只拉取」的链路完全跑通。
- 提交 `f9318692d`（deploy.sh 硬拦截 + compose 去掉 version）**未能推送**：本机无 GitHub 凭据
  （`fatal: could not read Username for 'https://github.com'`），需用户自己 `git push`。
  推之前服务器还会看到那条 `version is obsolete` 警告，无功能影响。
- 服务器侧的起步配置（用户待做）：后台「贡献上游 Key → 设置」开开关 + 点「启用重试（2 次）」写 `RetryTimes=2`。
  后者是号池无痕切换的前提，`RetryTimes` 默认 0 会导致 Key 出错时用户直接看到报错。

### 第三轮修改（用户反馈 4 点，2026-09-24 18:00）
1. **`docs_url` 修正**：sensenova 预设的 `DocsURL` 从 `https://token.sensenova.cn`（那是 API 端点）改为 `https://www.sensenova.cn/`（官网，取 Key 入口）。`BaseURL` 保持 `https://token.sensenova.cn/v1` 不动。
   - 关键机制：`GetOptions` 下发的是 `common.OptionMap`，而 `OptionMap` 在 `model/option.go:190` 通过 `config.GlobalConfig.ExportAllConfigs()` 注入了**配置默认值**，
     之后才被数据库里的值覆盖。所以：数据库没存过 `contributed_key_setting.providers` → 用代码默认值（改代码+重建镜像生效）；
     存过 → 数据库优先，**必须在后台 JSON 编辑器里改 docs_url**（改完立即生效，不用重建）。
2. **用户端提示删除**（用户明确不想让用户看到内部机制）：
   - 删 `By submitting you agree that the key is used to serve model calls for this site...`（连带 ShieldCheck 图标与导入）
   - 删「How it works」第 4 条 `Keys are checked periodically. If a key stops working, the quota it granted is reclaimed automatically.`（后续条目重新编号）
   - 删「My contributed keys」副标题 `Unavailable keys are removed from your daily quota automatically.`
   - 删除确认文案改为中性的 `The key will be removed from your list.`
3. **英文文案根因**：`settings-panel.tsx` / `all-keys-panel.tsx` 里 9 条文案漏了 zh.json 翻译（`Seamless failover`、`Retries: {{count}}`、`Enable retries (2 times)` 等），已全部补齐。
   - 沉淀：**新增前端文案后必须跑一次全量扫描脚本**（对 `features/**` 里的 `t('...')` 逐个比对 zh.json），否则中文界面会混英文。
4. **去掉「备注」输入框**：my-contribution-panel 移除 remark state / Input / 导入；后端 `addContributedKeyRequest.Remark` 保留（可选字段，不影响）。
5. **主页重做**：用户要求「模仿那个网站」，但现在这个太丑。用户选择「我另发链接给你」→ **等用户提供参考网址/截图**，勿自行猜测风格。

### 第四轮（18:05，用户发来参考站点截图 + 版本号问题）
- 用户贴了两张参考站点截图（首页 + FAQ 页），并指出**自己的站点多了一个版本号**要删。
- **版本号来源已定位**：`components/layout/components/public-header.tsx:237` 与 `app-header.tsx:118` 的
  `<SystemUpdateAction presentation='version' />`（组件在 `features/system-update/system-update-action.tsx`，
  `isAdmin` 为假时 `return null`，所以**只有管理员看得见**）。两处已删除并清理 import。
  后台「系统设置 → 维护」里的更新检查（`compact={false}`）保留，功能不受影响。
- **首页对照参考截图改造**（极简 hero + 横向数据条 + 3 列模型卡）：
  1. `hero.tsx`：删顶部徽章（脉冲小标签）与 API 终端演示（`HeroTerminalDemo` 因此在全仓库无人引用，
     文件保留 `features/home/components/hero-terminal-demo.tsx` 以便日后恢复）；次按钮默认不渲染
     （`hero_secondary_text` 为空即隐藏）；主按钮改实心蓝 `bg-blue-600`。
  2. `stats.tsx`：重写为**横向单行卡片** —— 左侧状态灯 + 标题，右侧四组指标，lg 以上 `divide-x` 分隔；
     每组三行（指标名 / 数值 / 说明）。`HomeLandingStatItem` 新增 `hint` 字段（后端 struct + 前端类型 + 装修表单三处同步）。
  3. `model-gallery.tsx`：删 uppercase 眉标；卡片 `p-4→p-5`、栅格去掉 `xl:grid-cols-4`；
     右上角状态圆点改为**厂商徽章**；底部三栏由「数值在上、标签在下」改为「标签在上、数值在下」。
- i18n：新增 5 条（`Live today` / `Across the gateway` / `Connected vendors` / `Model groups` / `API endpoints`），
  并顺手补了全量扫描发现的上游遗留 4 条漏翻译（`{{count}} ... deleted. Click "Save Settings" to apply.`）。
- 验证：`tsgo -b` 过滤测试文件后**本次改动文件零报错**（仅剩 `features/channels/api.ts` 与 `test-setup.ts` 两处 npm 版本漂移噪声）。
- **待用户确认（重要）**：参考站点数据条展示「调用次数 / Tokens 消耗 / 今日消耗 / 用户数」，模型卡片展示
  「今日调用 / 今日 Tokens」—— 这些属于**全站用量聚合**，本分支目前没有该接口（`/api/status` 不含请求量/Token 统计）。
  当前数据条沿用真实可得的网关数据（可用模型 / 供应商 / 分组 / 接口）。若确需全站统计，要新增带缓存刷新的公开接口，
  不能在首页同步聚合 `logs` 表（1核2G 会被拖垮）。

### 第五轮（18:20，用户要求「一比一复刻」参考站点）
用户给出三张完整截图（首页 hero + 数据条、9 张模型卡片、FAQ 尾部 + 交流社区 + 页脚），
要求「一比一复刻，只改样式和文案，删掉用不到的东西，主要是二开成共享号池」。

**新增后端（全站统计）**：
- `model/landing_stats.go`：`landing_visit_stats` 表（stat_date 主键 + visit_count），
  `AddLandingVisits`（先 UPDATE 累加，无记录则 INSERT，撞主键再退回累加）、`GetLandingVisits`、`CountActiveUsers`。
- `service/landing_stats.go`：内存快照 + 后台刷新任务。
  - 口径**全部是「当天」**：今日调用次数、今日输入/输出 Token、今日访问量、注册用户数、按模型的今日调用与 Token。
  - 关键判断依据：用户上一轮截图 18:00 显示 26,231，18:20 显示 26,345 —— 数值在涨，说明是当日数据而非累计，
    因此**不需要全表聚合**，`WHERE type=2 AND created_at >= 今日0点` 走 `idx_created_at_type` 索引即可，性能安全。
  - 缓存为空时同步刷新一次；之后由 `StartLandingStatsTask`（30 秒 tick、60 秒刷新）维护；
    访问量在内存累加、60 秒批量落库，避免首页每次打开都写库。
- `controller/landing_stats.go` + 路由：`GET /api/landing_stats`（公开，只下发聚合数字）、`POST /api/landing_visit`。
- `main.go` 启动 `service.StartLandingStatsTask()`；`model/main.go` 注册 `&LandingVisitStat{}` 迁移。

**模型标签**：`HomeLandingSetting.ModelsTags`（JSON: 模型名 -> 标签）+ `ParseHomeLandingModelTags`，
随 `/api/home_landing` 的 `models.tags` 下发，用于卡片右上角徽章（截图里的「深度推理」「长文本」等）。
默认留空，由管理员在「系统设置 → 内容 → Landing Page → Model badges」配置。

**前端**：
- `use-landing-stats.ts`：`useLandingStats`（60 秒轮询）+ `useLandingVisitReporter`（sessionStorage 去重上报）。
- `stats.tsx`：四项改为「调用次数 / Tokens 消耗（含输入·输出说明）/ 今日访问量 / 用户数量」；
  **统计接口不可用时自动退回网关能力数据**（可用模型/供应商/分组/接口），后端未升级也不会空一块。
- `model-gallery.tsx`：卡片底栏「上下文 / 今日调用 / 今日 Tokens」（读 `landing_stats.models`，
  同时用 `model_name` 与 `key` 两个键匹配，因为日志里的模型名与定价目录展示名可能不一致）；
  右上角改为后台配置的特性标签（浅蓝底 Badge）。
- `hero.tsx`：高亮渐变由蓝紫改为**橙红 → 紫**（`from-orange-500 via-rose-500 to-purple-600`），与截图一致。
- `community.tsx`：二维码改为「彩色标签 + 二维码 + 扫码说明」的卡片；去掉 uppercase 眉标。
- `footer.tsx`：右侧新增「服务运行中」状态灯（`useStatus()`）+ 免责说明文案。

### 部署排障（18:33）：服务器 `git pull` 被 deploy.sh 挡住
- 现象：服务器 `git pull` 报 `error: Your local changes to the following files would be overwritten by merge: deploy.sh`，
  但用户并没有改过脚本内容。**根因是文件权限位**：Windows 侧提交的 mode 是 100644，服务器 `chmod +x` 后变 100755，
  git 视作工作区被修改。根治：`git config core.fileMode false`（脚本保持可执行且以后不再冲突）。
- 重要区分：**服务器工作区落后 ≠ 服务是旧的**。镜像是从 GHCR 拉的，只要 Actions 构建成功，容器里就是最新代码。
  反之 pull 成功也不代表服务更新，还要 `./deploy.sh --registry`。
- 提交线：`89dfe7373`（贡献 Key/部署）→ `f9318692d`（deploy 硬拦截 + 去掉 compose 的 version）→
  `fa8d9744a`（首页一比一复刻 + 全站统计，**包含第三/四/五轮全部前端改动**）。服务器当时停在 89dfe7373。

### 第六轮（18:40）删除区块 + 修复导航挤压
- 用户要求删掉首页三块：**CTA**（「准备好简化你的 AI 集成了吗?」）、**三步快速上手**（HowItWorks）、**核心功能**（Features）。
  首页区块顺序变为：`Hero → Stats → ModelGallery → Faq → Community → Footer`。
- **导航挤压的根因**（值得记住）：`components/layout/components/public-header.tsx` 里滚动后容器宽度会收缩，
  `scrolled ? 'max-w-[52rem] px-3 pt-3' : 'max-w-7xl px-4 pt-0 md:px-6'` —— `52rem = 832px` 装不下
  「首页/控制台/模型/排行榜/常见问题/文档/关于」7 项 + logo + 语言/主题/通知/登录，于是导航文字被
  `truncate` 截成「主…」「控…」。
  修复：滚动后也保持 `max-w-7xl`（只保留胶囊外观，不再收窄）+ 导航项去掉 `min-w-0 truncate` 改 `whitespace-nowrap`。
- 顺手清理死代码（都是首页改造后没人引用的上游遗留）：`sections/cta.tsx`、`sections/how-it-works.tsx`、
  `sections/features.tsx`、`components/gateway-card.tsx`、`components/feature-item.tsx`、
  `components/connection-line.tsx`、`components/hero-terminal-demo.tsx`、`components/hero-buttons.tsx`、
  `components/icon-card.tsx`、`components/scrolling-icons.tsx`、`components/stat-item.tsx`、
  `lib/icon-mapper.tsx`、`constants.ts` —— 共 13 个文件。`features/home/` 从 23 个文件精简到 13 个。
  （清理方法：逐个 grep 组件名，只被自己引用的即为孤儿；删完再 grep 一次确认零残留。）

### 第七轮（18:46）docs_url 修正 + 导航精简 + 上游品牌清理
1. **docs_url 之前的修改没生效**：用户端仍显示 `https://token.sensenova.cn`（上游 API 端点）。
   根因不是代码，而是**数据库里已保存过 `contributed_key_setting.providers`（含旧地址），覆盖了代码默认值**。
   解决：在 `GetContributedKeyProviders()` 里加 `contributedLegacyDocsURLs` 兼容映射，命中旧值时直接返回官网
   `https://www.sensenova.cn/`，管理员不必再去后台改 JSON。（`BaseURL` 仍是 `https://token.sensenova.cn/v1`，那是服务端调用的上游地址，不能动。）
2. **导航精简为「模型 / 常见问题 / 用户面板」**：改 `lib/nav-modules.ts` 的 `DEFAULT_HEADER_NAV_MODULES`
   （`home/rankings/docs/about` 默认 false，只留 `pricing/help/console`），
   并在 `use-top-nav-links.ts` 里重排顺序为 pricing → help → console，把 Console 的标题键从 `'Console'`
   换成新增的 `'User Panel'`（避免影响后台侧边栏的「控制台」文案）。
   ⚠️ 该配置存在 option `HeaderNavModules` 里，**后台配过的话默认值不生效**，要提醒用户去后台关掉多余模块。
3. **Hero 主按钮**：去掉 `isAuthenticated ? Go to Dashboard : primaryText` 分支，固定显示「免费获取密钥」。
4. **清理上游品牌（用户可见位置）**：
   - `footer.tsx`：删 `ProjectAttribution`（© … New API + github 外链）、删 `fallbackColumns`（docs.newapi.pro 三列 + One API/Midjourney 链接）、
     `displayName` 不再回退到 `'New API'`；
   - `assets/logo.tsx` 的 `<title>New API</title>` → `Logo`；
   - `system-brand.tsx` 的 `'New API'` fallback 去掉；
   - `community.tsx` / `faq/index.tsx` 的默认文档链接 `https://docs.newapi.pro` 去掉（为空时不再渲染该入口）；
   - `errors/general-error.tsx` 删掉指向 `github.com/QuantumNous/new-api/issues` 的「报告问题」按钮；
   - `chat/lib/chat-links.ts` 的 AQBot 配置名 → `AI Gateway`；
   - `common/constants.go` 的 `SystemName` 默认值 → `"AI Gateway"`；`web/index.html` 的 title/meta → `AI Gateway`。
   - **保留** `id: 'new-api'` / `platform: 'new-api'`：那是 Cherry Studio / AionUi / DeepChat 等客户端的
     配置格式标识符，改了客户端会认不出。
5. **删除 `/about` 页面**（整页都是上游项目介绍）：`features/about/` + `routes/about/` + `routeTree.gen.ts` 的 9 处注册
   + `use-top-nav-links.ts` 的 about 分支。routeTree 用脚本精确删除并断言每处出现次数，删完 grep 确认零残留。















